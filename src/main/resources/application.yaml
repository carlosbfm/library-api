spring:
  application:
    name: library-api
  
  datasource:
    url: jdbc:postgresql://localhost:5433/library_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

api:
  security:
    token:
      secret: minha_senha_super_secreta_123